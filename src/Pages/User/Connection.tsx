import React from 'react';
import ConnectionForm from '../../Components/User/ConnectionForm';

const Connection = () => {
    return (
        <div>
            <ConnectionForm />
        </div>
    );
};

export default Connection;
